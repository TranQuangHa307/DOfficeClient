import React from 'react';

const ClientLayout = ({ children }) => {
    return (
        <>
            header Layout
            {children}
            footer Layout
        </>
    );
};

export default ClientLayout;
